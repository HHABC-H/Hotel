# 酒店管理系统后端接口文档（前端联调版）

## 1. 基础信息
- 协议：HTTP/HTTPS
- Base URL：`http://localhost:8080/api`
- 数据格式：`application/json`
- 认证方式：JWT Bearer Token
- 请求头：`Authorization: Bearer <token>`

## 2. 统一用户模型
系统使用统一用户表 `user`（员工 + 客户）：
- `id`
- `username`
- `password`
- `real_name`
- `phone`
- `id_card`
- `gender`
- `role`（`ADMIN` / `RECEPTIONIST` / `CLIENT`）
- `status`（`1` 启用 / `0` 禁用）
- `balance`（账户余额）
- `create_time`
- `update_time`

说明：
- `/customers/**` 操作的是 `role=CLIENT` 的用户记录。
- `order.customer_id` 对应 `user.id`，且应为 `CLIENT`。

## 3. Token 说明
登录成功后签发 token，claim 包含：
- `userId`
- `username`
- `role`
- `realName`
- `status`
- `iat`（签发时间）
- `exp`（过期时间）

当前 token 过期时间为 `1800` 秒（30 分钟）。

## 4. 权限矩阵
- 公开接口：`POST /auth/login`
- 已登录可访问：`POST /auth/logout`、`GET /auth/current-user`、`GET /orders/my`、`/profile/**`
- `ADMIN`：`/users/**`
- `ADMIN` + `RECEPTIONIST`：`/customers/**`、`/room-types/**`、`/rooms/**`、`/orders/**`
- `CLIENT`：`/bookings/**`
- `ADMIN` + `RECEPTIONIST` + `CLIENT`：`GET /rooms/browse`、`GET /rooms/{id}/detail`、`GET /rooms/available`

## 5. 统一返回结构
成功：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

失败：
```json
{
  "code": 400,
  "message": "错误信息",
  "data": null
}
```

分页 `data`：
```json
{
  "total": 100,
  "records": []
}
```

## 6. 认证接口 `/auth`

### 6.1 登录
- 方法：`POST`
- 路径：`/auth/login`
- 权限：公开

请求体：
```json
{
  "username": "admin",
  "password": "123456"
}
```

响应示例：
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "<jwt>",
    "tokenType": "Bearer",
    "expiresIn": 1800,
    "user": {
      "id": 1,
      "username": "admin",
      "realName": "系统管理员",
      "phone": "13800138000",
      "idCard": "110101199001011234",
      "gender": "UNKNOWN",
      "role": "ADMIN",
      "status": 1,
      "balance": 0.00
    }
  }
}
```

说明：
- 登录兼容明文/MD5/BCrypt 密码。
- 账号 `status=0` 会被拒绝登录。

### 6.2 登出
- 方法：`POST`
- 路径：`/auth/logout`
- 权限：已登录

### 6.3 当前用户
- 方法：`GET`
- 路径：`/auth/current-user`
- 权限：已登录

## 7. 用户管理接口 `/users`（ADMIN）

### 7.1 分页查询
- 方法：`GET`
- 路径：`/users`
- 参数：`pageNum`、`pageSize`、`username`、`role`、`status`

### 7.2 详情
- 方法：`GET`
- 路径：`/users/{id}`

### 7.3 新增
- 方法：`POST`
- 路径：`/users`

请求体示例：
```json
{
  "username": "staff01",
  "password": "123456",
  "realName": "前台A",
  "phone": "13900000000",
  "idCard": "110101199001011234",
  "gender": "F",
  "role": "RECEPTIONIST",
  "status": 1,
  "balance": 0.00
}
```

校验：
- `username` 唯一
- `idCard` 非空时唯一
- `balance` 不可为负数

### 7.4 更新
- 方法：`PUT`
- 路径：`/users/{id}`

### 7.5 删除
- 方法：`DELETE`
- 路径：`/users/{id}`

### 7.6 更新状态
- 方法：`PUT`
- 路径：`/users/{id}/status`

请求体：
```json
{
  "status": 0
}
```

## 8. 客户管理接口 `/customers`（ADMIN / RECEPTIONIST）
说明：底层是统一用户表中 `role=CLIENT` 的记录。

### 8.1 分页查询
- 方法：`GET`
- 路径：`/customers`
- 参数：`pageNum`、`pageSize`、`keyword`

`keyword` 模糊匹配：`realName`、`phone`、`idCard`、`username`

### 8.2 详情
- 方法：`GET`
- 路径：`/customers/{id}`

### 8.3 新增客户
- 方法：`POST`
- 路径：`/customers`

请求体示例：
```json
{
  "username": "client01",
  "password": "123456",
  "realName": "张三",
  "phone": "13800138000",
  "idCard": "110101199901011234",
  "gender": "M",
  "status": 1,
  "balance": 1000.00
}
```

说明：
- 自动写入 `role=CLIENT`
- `balance` 不可为负数

### 8.4 更新客户
- 方法：`PUT`
- 路径：`/customers/{id}`

### 8.5 删除客户
- 方法：`DELETE`
- 路径：`/customers/{id}`

## 9. 客房类型接口 `/room-types`（ADMIN / RECEPTIONIST）
- `GET /room-types`
- `GET /room-types/{id}`
- `POST /room-types`
- `PUT /room-types/{id}`
- `DELETE /room-types/{id}`

## 10. 客房接口 `/rooms`

### 10.1 后台管理接口（ADMIN / RECEPTIONIST）
- `GET /rooms`
- `GET /rooms/{id}`
- `POST /rooms`
- `PUT /rooms/{id}`
- `DELETE /rooms/{id}`
- `PUT /rooms/{id}/status`

### 10.2 前台可访问接口（ADMIN / RECEPTIONIST / CLIENT）
- `GET /rooms/available`
- `GET /rooms/browse`
- `GET /rooms/{id}/detail`

## 11. 订单接口 `/orders`

### 11.1 后台管理接口（ADMIN / RECEPTIONIST）
- `GET /orders`
- `GET /orders/{id}`
- `POST /orders`
- `PUT /orders/{id}`
- `DELETE /orders/{id}`（业务上是取消订单）
- `PUT /orders/{id}/pay`
- `PUT /orders/{id}/check-in`
- `PUT /orders/{id}/check-out`

创建/更新请求体：
```json
{
  "customerId": 3,
  "roomId": 2,
  "checkInDate": "2026-04-20",
  "checkOutDate": "2026-04-22",
  "remark": "尽量安静"
}
```

### 11.2 我的订单（已登录）
- 方法：`GET`
- 路径：`/orders/my`

## 12. 在线预订接口 `/bookings`（CLIENT）

### 12.1 创建预订
- 方法：`POST`
- 路径：`/bookings`

请求体：
```json
{
  "roomId": 2,
  "checkInDate": "2026-04-20",
  "checkOutDate": "2026-04-22",
  "remark": "尽量高楼层"
}
```

说明：
- `customerId` 自动取当前 token 的 `userId`
- 订单号前缀：`BK`
- 创建后状态为 `UNPAID`

### 12.2 我的预订
- 方法：`GET`
- 路径：`/bookings/my`

### 12.3 支付预订订单
- 方法：`PUT`
- 路径：`/bookings/{id}/pay`
- 权限：`CLIENT`（仅可支付自己的订单）

说明：
- 仅 `UNPAID` 订单可支付
- 支付时会扣减当前用户余额
- 余额不足返回 `400`

### 12.4 取消预订订单
- 方法：`DELETE`
- 路径：`/bookings/{id}/cancel`
- 权限：`CLIENT`（仅可取消自己的订单）

说明：
- 仅 `UNPAID` 订单支持在线取消
- 已完成订单不可取消

## 13. 个人资料接口 `/profile`（已登录）

### 13.1 获取个人资料
- 方法：`GET`
- 路径：`/profile`

### 13.2 更新个人资料
- 方法：`PUT`
- 路径：`/profile`

请求体示例：
```json
{
  "realName": "李四",
  "phone": "13900001111",
  "idCard": "110101199801011234",
  "gender": "F"
}
```

## 14. 业务状态与规则

### 14.1 订单状态
- `UNPAID`
- `PAID`
- `CANCELLED`
- `COMPLETED`

### 14.2 房间状态
- `AVAILABLE`
- `OCCUPIED`
- `MAINTENANCE`

### 14.3 冲突校验
创建/更新订单和在线预订时，系统会校验同一房间在目标时间段是否和 `UNPAID`/`PAID` 订单冲突。

### 14.4 关键业务约束
- 入住：仅 `PAID` 订单可办理，且房间必须是 `AVAILABLE`。
- 退房：仅 `PAID` 订单可办理；退房后订单改 `COMPLETED`，房间改 `AVAILABLE`。
- 取消：已 `COMPLETED` 订单不可取消；已入住场景不可取消。

