# 酒店管理系统开发文档(与当前代码一致)

## 1. 项目概述

### 1.1 项目目标
- 实现客房、订单、用户的数字化管理
- 支持员工端管理与客户端预订
- 提供基于 JWT 的统一认证与权限控制

### 1.2 当前后端实现范围
- 认证模块: 登录、登出、当前用户
- 用户管理: 管理员维护员工与账号
- 客户管理: 基于统一用户表中 `role=CLIENT` 的客户管理
- 客房类型与客房管理
- 订单管理: 创建、支付、入住、退房、取消、我的订单
- 客户端能力: 浏览房间、在线预订、我的预订、个人资料

## 2. 系统架构

### 2.1 架构模式
前后端分离: `Vue` + `Spring Boot` + `MySQL`

### 2.2 后端技术栈
- Spring Boot 2.7.x
- Java 17
- MyBatis-Plus
- Spring Security
- JJWT
- Maven

### 2.3 运行配置
- 服务端口: `8080`
- Context Path: `/api`
- 数据源: MySQL `hotel`

## 3. 数据模型设计(当前代码)

### 3.1 统一用户表 `user`
统一承载员工与客户。

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| username | VARCHAR(50) | 登录用户名，唯一 |
| password | VARCHAR(100) | 密码(新增或修改用户时按MD5存储) |
| real_name | VARCHAR(50) | 真实姓名 |
| phone | VARCHAR(20) | 手机号 |
| id_card | VARCHAR(20) | 身份证号，唯一(可空) |
| gender | VARCHAR(20) | 性别(`M/F/UNKNOWN`) |
| role | VARCHAR(20) | `ADMIN/RECEPTIONIST/CLIENT` |
| status | TINYINT | 1启用，0禁用 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

### 3.2 客房类型表 `room_type`
| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| type_name | VARCHAR(50) | 类型名称 |
| price | DECIMAL(10,2) | 价格/晚 |
| capacity | INT | 可住人数 |
| bed_type | VARCHAR(50) | 床型 |
| area | DECIMAL(6,2) | 面积 |
| description | TEXT | 描述 |
| status | TINYINT | 状态 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

### 3.3 客房表 `room`
| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| room_number | VARCHAR(20) | 房间号，唯一 |
| room_type_id | BIGINT | 客房类型ID |
| floor | INT | 楼层 |
| status | VARCHAR(20) | `AVAILABLE/OCCUPIED/MAINTENANCE` |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

### 3.4 订单表 `order`
| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| order_number | VARCHAR(50) | 订单号，唯一 |
| customer_id | BIGINT | 客户ID(对应 `user.id` 且角色应为CLIENT) |
| room_id | BIGINT | 客房ID |
| check_in_date | DATE | 入住日期 |
| check_out_date | DATE | 退房日期 |
| total_amount | DECIMAL(10,2) | 总金额 |
| status | VARCHAR(20) | `UNPAID/PAID/CANCELLED/COMPLETED` |
| remark | TEXT | 备注 |
| create_user_id | BIGINT | 创建人ID(员工或客户本人) |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

## 4. 认证与授权

### 4.1 JWT认证
登录成功签发Token，claim包含:
- `userId`
- `username`
- `role`
- `realName`
- `status`
- `iat`
- `exp`

当前配置的 token 过期时间: `1800` 秒(30分钟)。

### 4.2 权限控制(Spring Security)
- 公开: `POST /auth/login`
- 已登录通用: `POST /auth/logout`、`/auth/current-user`、`/profile/**`、`GET /orders/my`
- `CLIENT`专属: `/bookings/**`
- `ADMIN`专属: `/users/**`
- `ADMIN/RECEPTIONIST`: `/customers/**`、`/room-types/**`、`/rooms/**`、`/orders/**`
- 房间浏览接口(含客户端): `GET /rooms/browse`、`GET /rooms/{id}/detail`、`GET /rooms/available`

## 5. 核心业务规则

### 5.1 订单状态规则
- 状态: `UNPAID`、`PAID`、`CANCELLED`、`COMPLETED`
- 支付: 仅 `UNPAID -> PAID`
- 入住: 仅 `PAID` 订单可入住
- 退房: 仅 `PAID` 订单可退房并置为 `COMPLETED`
- 取消: `COMPLETED` 不可取消；已入住场景不可取消

### 5.2 房态联动
- 入住后: 房态 `AVAILABLE -> OCCUPIED`
- 退房后: 房态 `OCCUPIED -> AVAILABLE`

### 5.3 时间冲突校验
创建/更新订单和在线预订时，会校验同一房间在目标时间段是否与 `UNPAID/PAID` 订单冲突。

### 5.4 我的订单查询规则
- 当前用户为 `CLIENT`: 按 `customer_id = 当前userId`
- 当前用户为员工: 按 `create_user_id = 当前userId`

### 5.5 个人资料更新规则
- `PUT /profile` 更新 `idCard` 时，会校验身份证号唯一性(排除当前用户自身)。

## 6. API概览
- 认证: `/auth/*`
- 用户管理: `/users/*`
- 客户管理: `/customers/*`(底层为 `role=CLIENT` 的 user)
- 客房类型: `/room-types/*`
- 客房: `/rooms/*`
- 订单: `/orders/*`
- 在线预订: `/bookings/*`
- 个人资料: `/profile`

## 7. 项目结构(后端)
```text
Hotel/
├── src/main/java/com/hotel/
│   ├── common/
│   ├── config/
│   ├── controller/
│   ├── entity/
│   ├── mapper/
│   ├── security/
│   ├── service/
│   └── util/
├── src/main/resources/
│   └── application.yml
└── pom.xml
```

## 8. 测试建议(Apifox)
1. 先用 `/auth/login` 拿 token。
2. 在 Apifox 全局变量设置 `Authorization: Bearer {{token}}`。
3. 先测管理员链路: `/users`、`/room-types`、`/rooms`、`/orders`。
4. 再测客户端链路: `/rooms/browse`、`/bookings`、`/bookings/my`、`/orders/my`、`/profile`。
5. 覆盖异常用例:
   - 禁用用户登录
   - 客户访问管理接口(应403)
   - 订单时间冲突
   - 非法状态流转(未支付直接入住等)